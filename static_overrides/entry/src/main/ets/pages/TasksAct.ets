// TasksAct — layout-converted shell for tasks_act.xml.
// The actual page is TasksPage.ets which includes navigation drawer + full task list UI.
@Component
struct TasksAct {
  build() {
    Column() {
    }
    .width('100%')
    .height('100%')
  }
}
